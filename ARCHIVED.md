# Active Inebotten repository

This file supersedes the stale 2026-06-02 archive marker. The previously named
canonical checkout, `/Users/reidar/Projectos/inebotten-discord`, does not exist, and
this repository uses the canonical `Reedtrullz/inebotten-discord` remote.

The launchd-managed selfbot runtime uses `/Users/reidar/Projectos/inebotten` as its
working directory. That runtime checkout can contain preserved work and private local
artifacts. Before switching branches, cleaning files, or removing it, verify that no
running process owns the checkout and preserve any unmerged work.

This status correction does not claim current Discord behavior or manual acceptance.
